# Subagent

状态：设计与调研，尚无可运行实现。

## 目标

提供一个通用、轻量的 Pi 子 Agent 执行扩展，以及一份说明如何委派任务的通用 Skill。任务职责由每次调用定义，不维护预设角色库。

## 目标能力

- 启动子任务，显式配置任务、工作目录、上下文和工具权限。
- 查询状态、返回结果、取消任务、报告错误。
- 并行执行与并发限制。
- 基本运行记录和完成通知。
- 为未来的监控面板提供公开状态与事件接口。

## 初始默认策略（待 SDK 验证）

- 独立上下文，写权限显式授予。
- 默认禁止子 Agent 继续派生。
- 同一工作目录最多一个写入者；并行写入需要显式隔离。
- 子任务遇到超出授权的决定时报告阻塞。
- 子任务的完成报告必须说明验证情况与未完成项。
- 执行失败不静默切换执行方式。

## 当前不包含

- todo、update_plan 或 Plan Mode。
- spec、plan、review cycle 等业务流程。
- 预设角色、多顾问讨论或自动模型路由。
- 专用监控面板（后续目标）。
- 跨 CLI 执行器、定时任务和复杂工作流引擎。

## 下一步

1. 完整阅读 Pi SDK、扩展文档及官方 Subagent 示例。
2. 对照 pi-subagents 的执行、通知、上下文、权限和生命周期实现。
3. 验证最小子会话执行、结果提取、取消、异常和资源释放。
4. 编写最小设计与验收测试，再开始实现。
